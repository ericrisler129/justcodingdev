(function ($) {

  // Example Behaviors to be used for HCPRO sites

  /*
  Drupal.theme.prototype.hcproFunction = function (target) {
  };

  Drupal.behaviors.hcproBehavior = {
    attach: function () {
      $('body').once('event-description', function () {
        Drupal.theme('hcproFunction', $(this));
      });
    }
  };
  */


})(jQuery);
